# Checklist

- [ ] MD5 已从 `core/distance.py` 与 `core/solver.py` 中完全移除，缓存键使用 SHA-256 / blake2b
- [ ] API Key 中间件在未配置密钥时默认拒绝非健康检查请求（401），并可通过环境变量显式放行
- [ ] FastAPI 配置了请求体大小限制，超大请求返回 413
- [ ] 全局 500 异常处理器仅返回脱敏消息，服务端日志记录完整异常
- [ ] callback_url SSRF 校验覆盖私有 IP、localhost、内网域名、非法协议，并附有单测
- [ ] Ruff 在核心后端源码中无剩余错误（允许对旧 Streamlit 入口 `web_app.py`、`frontend/app.py` 的不可修复警告单独记录）
- [ ] MyPy 在 `core/distance.py`、`core/solver.py`、`optimization/dynamic.py`、`data_types/solution.py` 中无新增/残留关键错误
- [ ] Radon 识别的高复杂度函数（`core/cost.py:_get_vehicle_params_cached_impl`、`utils/validation.py:validate_customer`、`analysis/comparison.py:ScenarioComparison.compare_solutions`）已拆分或复杂度下降
- [ ] 距离矩阵缓存键生成稳定、可预测，缓存命中率测试通过
- [ ] `SolverPool` 无效池化代码已清理，解决方案缓存保留且测试通过
- [ ] 稀疏距离矩阵路径不再导致 `build_time_matrix` 异常
- [ ] 场景数据使用 SQLAlchemy + SQLite 持久化，CRUD 接口工作正常，API 重启后数据可恢复
- [ ] 前端路由使用 `React.lazy + Suspense` 实现代码分割
- [ ] Vite 生产构建主 JS chunk < 500 KB
- [ ] 全局 ErrorBoundary 已接入路由
- [ ] `TabsTrigger` 可访问性/自动化识别问题已修复
- [ ] 前端 API 错误提示统一，不直接暴露后端原始异常
- [ ] CI 配置正确调用 `ruff check .` 并包含前端构建/测试步骤
- [ ] `ARCHITECTURE.md` 与 `CHANGELOG.md` 已更新
- [ ] 后端 `pytest tests` 与前端 `npm run build && npm run test` 全部通过
